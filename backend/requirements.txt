# Core web framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
gunicorn>=23.0.0
websockets>=14.0
httpx>=0.28.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
orjson>=3.10.0
# Wave 3L Fix 5 (persona-86/90): drop-in asyncio replacement, 2–4× faster
# on high-frequency async I/O. Installed at the top of main.py.
uvloop>=0.19.0

# Auth
PyJWT>=2.8.0
bcrypt>=4.0.0
passlib[bcrypt]>=1.7.4
# TOTP / 2FA skeleton — Wave 2I Fix 2 (P81-1). ``pyotp`` is small (~30KB) and
# has zero runtime deps beyond the stdlib; no hidden bloat.
pyotp>=2.9.0

# Database
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
# psycopg2-binary is the sync driver used by data/ingestion/trade_ledger.py's
# SQLAlchemy sync engine (postgresql://... resolves to psycopg2 by default).
# Without this the TradeLedger falls back to an in-memory cache at worker
# boot with "No module named 'psycopg2'" and writes go nowhere durable.
psycopg2-binary>=2.9
alembic>=1.14.0
redis>=5.2.0

# Task queue
celery[redis]>=5.4.0

# Data science core
pandas>=2.2.0
numpy>=2.0.0
polars>=1.16.0
scipy>=1.14.0
statsmodels>=0.14.0   # Engle-Granger ADF used by backend/indicators/stats.py

# ML
lightgbm>=4.5.0
xgboost>=2.1.0
scikit-learn>=1.6.0
optuna>=4.1.0
hmmlearn>=0.3.3

# Trading
polygon-api-client>=1.14.0
alpaca-py>=0.33.0

# Claude AI
# B-86: upper-bound the SDK to the currently-validated minor. Minor
# bumps (0.40 → 0.41) can change client signatures or response shapes
# silently; we want CI to break on the next ``pip install`` rather than
# production to break on the next deploy. Bump the ceiling as part of a
# deliberate SDK-upgrade PR.
anthropic>=0.40.0,<0.41.0

# Parquet cache for data providers
pyarrow>=17.0.0

# Market calendars (NYSE/NASDAQ sessions, holidays, half-days)
pandas-market-calendars>=4.4.0

# Technical analysis
pandas-ta>=0.3.14b1

# Options math
py_vollib>=1.0.1

# Notifications
python-telegram-bot>=21.9

# Fundamentals (install separately - heavy)
# edgartools>=3.4.0
# openbb>=4.4.0
# quantstats>=0.0.64
