[pytest]
# pytest-asyncio mode: 'strict' requires @pytest.mark.asyncio on each coroutine
# (our chosen style). 'auto' would collect every async def as a test which is
# too aggressive for a codebase with helper coroutines sitting in test modules.
asyncio_mode = strict
